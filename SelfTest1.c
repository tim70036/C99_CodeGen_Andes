#pragma token off

int a = 1;
const int a = 1;
int b = 1 + 1;
const char b = "aa";
const char b = 'a';
const bool b = true;
char a[100][100][3] = {a,a[3+2]+1*2%3+2||3&&2+"2">'3'}, b[1][2] = {};
int main()
{
    int a = 1;
    const char b = 'a';
    const bool b = true;

    b = func();
    b[func()][1+2+'3' || "what" != a[3] + func(a, 1+1, a && b - c, d++, "???"+3 , true)][false > 1] = func(a, 1+1, a && b - c, d++, "???"+3 , true);

    if(a == 1)
    {
        int a = 1;
        if(a+2*b >= 1)
        {

        }
        else
        {

        }
    }

    for(;;)
    {

    }

    for(-a+func() ; a < 3 ;a++)
    {
        int a = 1;
        a = 1;
        for( ; ; a++)
        {
            for(; func() ; )
            {
                a[func()][b][3] = -(a[func()][b][3]);
            }
        }
    }



    while(1 && 1 || 3)
    {
        int a = 1;
        while(a <= 3)
        {
            int a = 1;
            break;
        }
    }

    do {
        do {

        } while(func() || a[3][3]);
        continue;
    } while(func() != 3);

    switch (a) {
        case 1:

        case '3':
            c = a + 33;
            if(c == 1)
            {
                int c = a + 1 + b;
                a = a;
            }

        default:
            break;
    }

    switch (bac) {
        case 1:

    }

    switch (a) {
        case '3':
            switch (bac) {
                case 1:

            }
        default:
    }

    return 0;
    return;
}

int c = a + 1 + b;

int func(int d, char e, bool f, double g, int h[0][33][2]);

double i[0][1][2][3];

char h = 'h', j[1][2] = {}, k[4] = {'k' , 'k', 'k', 'k'};
